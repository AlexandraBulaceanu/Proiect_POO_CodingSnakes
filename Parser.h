#pragma once
#define Parser_H
#include <string>
#include <vector>
using namespace std;

class Parser
{
public:

    static string removeFrontSpaces(string&);

    static string getFirstWord(string&);

    static string removeFirstWord(string&);

    static string removeSpaces(string&);

};

